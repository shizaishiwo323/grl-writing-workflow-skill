interface:
  display_name: "GRL Writing Workflow"
  short_description: "GRL mechanism-writing workflow"
  default_prompt: "Diagnose the manuscript stage first, then apply the matching GRL writing workflow, prompt template, and teacher-derived revision principles."
